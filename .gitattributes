# BGA's server is Linux and we re-upload working-tree files via SFTP — keep LF.
* text=auto eol=lf

# Binary assets (never normalize)
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
